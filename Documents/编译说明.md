# 编译说明

> 编译流程的**可执行步骤**已封装为 skill: `firmware-build`。
> 本文档提供编译环境、配置、宏定义、常见错误等**参考信息**。

## 编译环境

| 工具 | 说明 |
|------|------|
| IAR Embedded Workbench for 8051 | 版本 8.10 或更高 |
| Z-Stack 3.0.2 | 已集成在项目仓库中 |
| Python 3 | 运行 hex2bin.py 转换工具、burn.py 一键烧录脚本 |

## IAR 安装路径

默认安装路径（如不同请替换）：

```
C:\Program Files (x86)\IAR Systems\Embedded Workbench\common\bin\IarBuild.exe
```

> 注意：路径中**不含版本号**，直接是 `Embedded Workbench\common\bin`。

## 工程文件

```
Projects/zstack/HomeAutomation/HGZBSwitch/CC2530DB/HGZBSwitch.eww
```

> v1.0.0 起从官方 SampleSwitch 工程独立为 HGZBSwitch 工程，剥离了 Z-Stack 官方示例残留代码。

## 编译配置 (Configuration)

| 配置名 | 用途 | 当前状态 |
|--------|------|----------|
| CoordinatorEB | 协调器固件 | 不使用 |
| **RouterEB** | 路由器固件（无OTA） | **当前使用** |
| EndDeviceEB | 终端设备固件 | 不使用 |
| RouterEB - OTAClient | 路由器+OTA客户端 | 不使用（256KB CC2530 不支持 OTA） |
| EndDeviceEB - OTAClient | 终端+OTA客户端 | 不使用 |

> 当前使用 **RouterEB** 配置编译（非OTA），设备角色为 Router。

## CLI 编译命令

```powershell
$iar = "C:\Program Files (x86)\IAR Systems\Embedded Workbench\common\bin\IarBuild.exe"
& $iar "d:\vc\Z-Stack\Projects\zstack\HomeAutomation\HGZBSwitch\CC2530DB\HGZBSwitch.ewp" -build "RouterEB"
```

> OTA 配置已弃用（CC2530 256KB Flash 空间不足以支持 Z-Stack Router 协议栈 + OTA Client，详见 [CHANGELOG.md](../CHANGELOG.md) v1.0.0 说明）。

## 编译产物

```
Projects/zstack/HomeAutomation/HGZBSwitch/CC2530DB/RouterEB/Exe/
```

| 文件 | 说明 |
|------|------|
| HGZBSwitch.hex | Intel HEX 格式固件（IAR 输出） |
| HGZBSwitch.bin | BIN 格式（hex2bin.py 或 burn.py 转换后，烧录用） |

> 编译后推荐直接使用 `firmware-burn` skill 或 [burn.py](烧录说明.md) 一键烧录，自动完成 hex→bin 转换并烧录。

## 关键预定义宏

以下宏在 IAR 工程配置中定义，影响编译行为：

| 宏 | 配置 | 说明 |
|----|------|------|
| `ZCL_REPORTING_DEVICE` | RouterEB | 启用 ZCL 属性上报功能（`zcl_SendReportCmd` 可用） |
| `ZG_BUILD_RTR_TYPE` | RouterEB | 设备类型为 Router |
| `CC2530` | 所有配置 | 目标芯片 |
| `HAL_KEY=FALSE` | RouterEB | 禁用 HAL_KEY 模块（避免 P2.0/P0_6 引脚冲突，v1.0.0 重构引入） |

> v1.0.0 重构后已移除 `OTA_CLIENT` 宏定义和相关代码。

## 常见编译错误

### 1. `Function "zcl_SendReportCmd" declared implicitly`

**原因**: 缺少 `ZCL_REPORTING_DEVICE` 宏定义

**修复**: 在 `HGZBSwitch.ewp` 对应 configuration 的 `CCDefines` 中添加 `<state>ZCL_REPORTING_DEVICE</state>`

### 2. 编译成功但无 .hex 文件输出

**原因**: IAR 工程配置的 ExtraOutputFormat 设置错误（如输出 .sim 而非 .hex）

**修复**: 在 `HGZBSwitch.ewp` 中将对应配置的 `ExtraOutputFile` 设为 `HGZBSwitch.hex`，`ExtraOutputFormat` 设为 `23`（intel-extended）

## 固件版本号

固件版本号定义在 `zcl_samplesw_data.c` 中，发版时需更新。**版本号规则、当前版本、发版清单**见：
- [项目文档索引.md](项目文档索引.md) — 当前版本号、发版更新清单
- [固件版本更新规则.md](固件版本更新规则.md) — 版本号规则定义、历史版本表
